To setup database connection:

1. Create a .env file at root directory
2. Mention below given environment variables there with same naming convention given below:
    
    DB_URI=<MONGODB URL>
    JWT_SECRET=<Secret key to encrypt data>
    JWT_EXPIRE=<Token expiration time>

Note: Without setting up this you can't run code.

Thank You